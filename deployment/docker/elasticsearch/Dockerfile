# File: deployment/docker/elasticsearch/Dockerfile
# Purpose: Builds the Elasticsearch container used for SHIELD telemetry and detection storage.

FROM docker.elastic.co/elasticsearch/elasticsearch:8.12.0